#include <stdio.h>

int main()
{
  int a, i, fact = 1;

  printf("Escribe un numero que desee saber su factorial\n");
  scanf("%d", &a);

  for (i = a; i > 1 ; i--){
    fact = fact * i;
}
  printf("El factorial de %d = %d\n", a, fact);
  return 0;
}
~
~
